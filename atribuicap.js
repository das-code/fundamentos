const a = 7
let b = 3

b += a // Atribuicao aditiva (b = b + a)
console.log(b)

b -= a // Atribuicao subtrativa (b = b - a)
console.log(b)

b *= a // (b = b * a)
console.log(b)

b /= a // (b = b / a)
console.log(b)

b %= a // (b = b % a)
console.log(b)